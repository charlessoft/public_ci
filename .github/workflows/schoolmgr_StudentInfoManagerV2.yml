name: schoolmgr_StudentInfoManagerV2


on:
  repository_dispatch:
    types:
      - schoolmgr_StudentInfoManagerV2
  push:
    branches: [ '*' ]
  pull_request:
  release:
    types: [ created, edited ]
  workflow_dispatch:
    inputs:
      gva_version:
        required: true
        type: string
env:
# game
  DEFAULT_DDING_SECRET: 'SECbe4003c4d14c740788cf9383fa1d8d77c029a03e6df99dde528274b9bcc17926'
  DEFAULT_DDING_TOKEN: 'fd87df4f83611b4c4cf31d5ad48f890f9603b57b1cac3d52fb80df8b86a5b629'
jobs:
  init:
    if: github.repository_owner == 'charlessoft'
    runs-on: ubuntu-latest
    steps:
      - name: init
        run: |
          echo "charlessoft"
          echo "${{ toJson(github.event.client_payload) }}"
          echo "charlessoft11"
          echo "${{ toJson(github.event.client_payload) }}" > client_payload.json


      - name: Set SSH
        run: |
          # 提取第一个 SSH key
          FIRST_KEY='${{ github.event.client_payload.ssh_keys[0] }}'
          
          # 创建 .ssh 目录并保存 SSH key
          mkdir -p ~/.ssh/
          echo "$FIRST_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # 添加 GitHub 到 known_hosts
          ssh-keyscan github.com >> ~/.ssh/known_hosts
      # - name: Setup Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '16.14.0'

      # - name: Install dding on non-Windows
      #   if: runner.os != 'Windows'
      #   run: |
      #     pip install dding
      #     mkdir -p ~/.dding
      #     dding_secret="${{ github.event.client_payload.dding_token.secret || env.DEFAULT_DDING_SECRET }}"
      #     dding_token="${{ github.event.client_payload.dding_token.token || env.DEFAULT_DDING_TOKEN }}"
      #     cat <<EOF > ~/.dding/config.json
      #     [
      #         {
      #             "group": "default",
      #             "secret": "$dding_secret",
      #             "token": "$dding_token"
      #         }
      #     ]
      #     EOF

      # - name: clone git code
      #   run: |
      #    git clone https://oauth2:${{ github.event.client_payload.gitee_token  }}@gitee.com/charlesabc/StudentInfoManagerV2.git ./StudentInfoManagerV2
      #    git clone https://oauth2:${{  github.event.client_payload.gitee_token  }}@gitee.com/charlesabc/myutils.git ./StudentInfoManagerV2/myutils
      # - name: Setup Debug Session
      #   uses: csexton/debugger-action@master
  frontend:
    if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'release'  || github.event_name == 'repository_dispatch'
    name: Frontend node ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.14.0]
    steps:
      - name: Check out branch
        uses: actions/checkout@v2

      - name: Clone ${{  github.event.client_payload.project_name }}
        run: |
         git clone https://oauth2:${{ github.event.client_payload.gitee_token  }}@gitee.com/charlesabc/StudentInfoManagerV2.git ./${{  github.event.client_payload.project_name }}
         git clone https://oauth2:${{  github.event.client_payload.gitee_token  }}@gitee.com/charlesabc/myutils.git ./${{  github.event.client_payload.project_name }}/myutils

          
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      # - name: Set SSH
      #   run: |
      #     # 提取第一个 SSH key
      #     FIRST_KEY=$(echo "${{ toJson(github.event.client_payload.ssh_keys) }}" | jq -r '.[0]')
          
      #     # 创建 .ssh 目录并保存 SSH key
      #     mkdir -p ~/.ssh/
      #     echo "$FIRST_KEY" > ~/.ssh/id_rsa
      #     chmod 600 ~/.ssh/id_rsa
          
      #     # 添加 GitHub 到 known_hosts
      #     ssh-keyscan github.com >> ~/.ssh/known_hosts
  
  #     - name: Set SSH
  #       env:
  #         KEY: ${{ secrets.SSH_KEY }}
  #       run: |
  #         echo $KEY
  #         mkdir -p ~/.ssh/ && echo "$KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
  #         ssh-keyscan github.com >> ~/.ssh/known_hosts
  
      - name: Get current branch name
        id: get_branch
        run: |
          echo "BRANCH=$(git rev-parse --abbrev-ref HEAD)" >> $GITHUB_ENV
          echo "UTILS_DIR=${PWD}/${{ github.event.client_payload.project_name }}/myutils" >> $GITHUB_ENV
          echo "WORK_ROOT_DIR=${PWD}/${{  github.event.client_payload.project_name }}" >> $GITHUB_ENV
          echo "==========="
          echo $(git rev-parse --abbrev-ref HEAD)
          echo "==========="

      - name: Generate JENKINS.env
        run: |
          echo "export BRANCH=$BRANCH " >> $WORK_ROOT_DIR/.JENKINS.env
          echo "export UTILS_DIR=$UTILS_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
          echo "export WORK_ROOT_DIR=$WORK_ROOT_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
          cat $WORK_ROOT_DIR/.JENKINS.env
        working-directory: ./${{ github.event.client_payload.project_name }}

  #     - name: Generate git commit
  #       run: |
  #          bash $WORK_ROOT_DIR/myutils/quickspider/scripts/build_web.sh $WORK_ROOT_DIR/.JENKINS.env

      - name: Build test
        shell: bash
        run: |
          pwd
          ls
          echo "========="
          source ${UTILS_DIR}/utils.sh
          cd webui
          npm install
          cd -
          # 发布到麦小
          bash ${WORK_ROOT_DIR}/myutils/StudentInfoManagerV2/scripts/build_server.sh $WORK_ROOT_DIR/.JENKINS.env || CheckLastResult

          # 发布到观澜
          bash ${WORK_ROOT_DIR}/myutils/StudentInfoManagerV2/scripts/guanlan/deploy_server.sh $WORK_ROOT_DIR/.JENKINS.env || CheckLastResult
        working-directory: ./${{ github.event.client_payload.project_name }}


        
  #     - name: Set SSH
  #       env:
  #         KEY: ${{ secrets.SSH_KEY }}
  #       run: |
  #         echo $KEY
  #         mkdir -p ~/.ssh/ && echo "$KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
  #         ssh-keyscan github.com >> ~/.ssh/known_hosts

  #     # - name: Setup Debug Session
  #     #   uses: csexton/debugger-action@master

  #     - name: publish web
  #       run: |
  #         # echo ${{ env.branch_name }} 
  #         # echo "export BRANCH=${{ env.branch_name }} " >> .JENKINS.env
  #         # find . -name "publish_web.sh"
  #         # echo "================="
  #         # cat .JENKINS.env
  #         bash $WORK_ROOT_DIR/myutils/quickspider/scripts/publish_web.sh $WORK_ROOT_DIR/.JENKINS.env

  # backend:
  #   if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'release' || github.event_name == 'repository_dispatch'
  #   name: Backend go
  #   runs-on: macos-latest
  #   strategy:
  #     matrix:
  #       go-version: [1.22]
  #   steps:
  #     - name: Set up Go ${{ matrix.go-version }}
  #       uses: actions/setup-go@v1
  #       with:
  #         go-version: ${{ matrix.go-version }}
  #       id: go

  #     - name: Check out branch
  #       uses: actions/checkout@v2


  #     - name: Clone quickspider-admin
  #       run: |
  #         git clone https://oauth2:${{ secrets.MYUSER_TOKEN  }}@github.com/charlessoft/quick-spider-admin.git ./quick-spider-admin
  #         #git clone https://oauth2:${{ secrets.GITEE_TOKEN  }}@gitee.com/charlesabc/myutils.git ./quick-spider-admin/myutils
          


  #     - name: Get current branch name
  #       id: get_branch
  #       run: |
  #         echo "BRANCH=$(git rev-parse --abbrev-ref HEAD)" >> $GITHUB_ENV
  #         echo "UTILS_DIR=${PWD}/quick-spider-admin/myutils" >> $GITHUB_ENV
  #         echo "WORK_ROOT_DIR=${PWD}/quick-spider-admin" >> $GITHUB_ENV
  #         echo "==========="
  #         echo $(git rev-parse --abbrev-ref HEAD)
  #         echo "==========="

  #     - name: Generate JENKINS.env
  #       run: |
  #         echo "export BRANCH=$BRANCH " >> $WORK_ROOT_DIR/.JENKINS.env
  #         echo "export UTILS_DIR=$UTILS_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
  #         echo "export WORK_ROOT_DIR=$WORK_ROOT_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
  #         cat $WORK_ROOT_DIR/.JENKINS.env
  #       working-directory: ./quick-spider-admin


  #     - name: Download dependencies
  #       run: |
  #         make build-server-local
  #       working-directory: ./quick-spider-admin

  #     # - name: Download dependencies
  #     #   run: |
  #     #     go get -v -t -d ./...
  #     #     if [ -f Gopkg.toml ]; then
  #     #         curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
  #     #         dep ensure
  #     #     fi
  #     #   working-directory: ./quick-spider-admin/server

  #     # - name: Test and Build
  #     #   run: |
  #     #     go build -v -race
  #     #     make 
  #     #     mv server quickspiderserver && tar zcvf quickspiderserver.tar.gz quickspiderserver ./config.yaml ./resource 
  #     #   working-directory: ./quick-spider-admin/




  #     - name: Set SSH
  #       env:
  #         KEY: ${{ secrets.SSH_KEY }}
  #       run: |
  #         echo $KEY
  #         mkdir -p ~/.ssh/ && echo "$KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
  #         ssh-keyscan github.com >> ~/.ssh/known_hosts






  #     - name: publish server
  #       run: |
  #         bash ./quick-spider-admin/myutils/quickspider/scripts/publish_server.sh $WORK_ROOT_DIR/.JENKINS.env



  # deploy:
  #     if: github.event_name == 'push' || github.event_name == 'pull_request' || github.event_name == 'release'  || github.event_name == 'repository_dispatch'
  #     name:  deploy
  #     runs-on: ubuntu-latest
  #     needs:
  #       - init
  #       - backend
  #       - frontend
  #     steps:
  #       - name: Check out branch
  #         uses: actions/checkout@v2
  
  #       - name: Clone quickspider-admin
  #         run: |
  #           git clone https://oauth2:${{ secrets.MYUSER_TOKEN  }}@github.com/charlessoft/quick-spider-admin.git ./quick-spider-admin
  #           #git clone https://oauth2:${{ secrets.GITEE_TOKEN  }}@gitee.com/charlesabc/myutils.git ./quick-spider-admin/myutils
  
  
  #       - name: Install dding on non-Windows
  #         if: runner.os != 'Windows'
  #         run: |
  #           pip install dding
  #           mkdir -p ~/.dding
  #           dding_secret="${{ github.event.client_payload.dding_secret || env.DEFAULT_DDING_SECRET }}"
  #           dding_token="${{ github.event.client_payload.dding_token || env.DEFAULT_DDING_TOKEN }}"
  #           cat <<EOF > ~/.dding/config.json
  #           [
  #               {
  #                   "group": "default",
  #                   "secret": "$dding_secret",
  #                   "token": "$dding_token"
  #               }
  #           ]
  #           EOF
  
  
  #       - name: Get current branch name
  #         id: get_branch
  #         run: |
  #           echo "BRANCH=$(git rev-parse --abbrev-ref HEAD)" >> $GITHUB_ENV
  #           echo "UTILS_DIR=${PWD}/quick-spider-admin/myutils" >> $GITHUB_ENV
  #           echo "WORK_ROOT_DIR=${PWD}/quick-spider-admin" >> $GITHUB_ENV
  #           echo "==========="
  #           echo $(git rev-parse --abbrev-ref HEAD)
  #           echo "==========="
  
  #       - name: Generate JENKINS.env
  #         run: |
  #           echo "export BRANCH=$BRANCH " >> $WORK_ROOT_DIR/.JENKINS.env
  #           echo "export UTILS_DIR=$UTILS_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
  #           echo "export WORK_ROOT_DIR=$WORK_ROOT_DIR" >> $WORK_ROOT_DIR/.JENKINS.env
  #           cat $WORK_ROOT_DIR/.JENKINS.env
  #         working-directory: ./quick-spider-admin
  
  
  #       - name: Set SSH
  #         env:
  #           KEY: ${{ secrets.SSH_KEY }}
  #           # HOST: ${{ secrets.HOST }}
  #           # USER: ${{ secrets.USER }}
  #           # PROT: ${{ secrets.PROT }}
  #           # MKDIRTEST: ${{ secrets.MKDIRTEST }}
  #         run: |
  #           echo $KEY
  #           mkdir -p ~/.ssh/ && echo "$KEY" > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa
  #           ssh-keyscan github.com >> ~/.ssh/known_hosts


  #       # - name: Setup Debug Session
  #       #   uses: csexton/debugger-action@master
          
  #       - name: Deploy 
  #         run: |
  #           echo "ok"
  #           bash $WORK_ROOT_DIR/myutils/quickspider/scripts/quickspider/docker_deploy.sh $WORK_ROOT_DIR/.JENKINS.env



  #       - name: Check Status
  #         if: ${{ success() }}
  #         shell: pwsh
  #         run: |
  #           $repoName = $env:GITHUB_REPOSITORY.Split('/')[-1]
            
  #           Write-Host "Workflow succeeded for $repoName!"
  #           dding "github action success :) for quickspider"
          

  #       - name: Check Status
  #         if: ${{ failure() }}
  #         shell: pwsh
  #         run: |
  #           $repoName = $env:GITHUB_REPOSITORY.Split('/')[-1]
  #           Write-Host "Workflow failed for $repoName!"
  #           dding "github action fail :( quickspider"
